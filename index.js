module.exports = Maki;

function Maki (options) {
  // expect data, state, ux
  // i dunno about ui... i'm not sure if it connects here
  // it might connect to a running instance
  // as opposed to at application definition time
}
